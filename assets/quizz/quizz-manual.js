function initQuizz() {

    let showPreviousBtn = showPreviousBtn;
    let surveyIdentifier = surveyIdentifier;
    let pathToSaveResult = pathToSaveResult;
    let nextButtonMsg = nextButtonMsg;
    let previousButtonMsg = previousButtonMsg;
    let submitButtonMsg = submitButtonMsg;
    let firstPageIsPreambule = firstPageIsPreambule;
    let surveyStepIdentifier = surveyStepIdentifier;
    let validationStepFunction = validationStepFunction;
    let enableAutoNext = enableAutoNext;
    let useTimer = useTimer;
    let durationOfStepTimer = durationOfStepTimer;
    let durationOfGlabalTimer = durationOfGlabalTimer;

}